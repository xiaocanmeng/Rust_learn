# MCP 使用最佳实践

## 1 在 public 仓库中进行自动写入
public 仓库允许自动写入，适合测试文档生成与自动化操作。

## 2 在 private 仓库中人工提交
private 仓库的写入会被安全层阻断，建议人工复制内容后提交。

## 3 使用独立分支
建议为 MCP 相关内容创建独立分支，例如 dev 或 mcp docs，以隔离主分支。

## 4 分阶段输出和写入
先让模型生成大纲，再生成正文，减少一次性写入长度，降低失败率。